module Interface.ManagedContract where

-- View type for the ManagedContract interface
data ManagedContractView = ManagedContractView
  with
    uuid : Text
    user : Party
  deriving (Eq, Show)

-- This is a marker interface that any contract can implement to be managed by ManagedContractFactory
interface ManagedContract where
  viewtype ManagedContractView
